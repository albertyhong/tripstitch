<h1>Listing trip_destinations</h1>

<table>
  <tr>
    <th>Trip</th>
    <th>Destination</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trip_destinations.each do |trip_destination| %>
  <tr>
    <td><%= trip_destination.trip_id %></td>
    <td><%= trip_destination.destination_id %></td>
    <td><%= link_to 'Show', trip_destination %></td>
    <td><%= link_to 'Edit', edit_trip_destination_path(trip_destination) %></td>
    <td><%= link_to 'Destroy', trip_destination, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trip destination', new_trip_destination_path %>
